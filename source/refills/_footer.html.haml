%footer.footer
  .wrapper
    .logo
      = image_tag 'images/interactive-resources-logo-white.png'
    %dl
      %dt 800-241-4474 | HELLO@IRTALENT.COM
      %dd © 2015 INTERACTIVE RESOURCES, LLC


= link_to 'Scroll to top', '#', class: 'scrollToTop'

%section.easter-egg
  .wrapper
    = image_tag 'images/easter-egg-type.png'

:javascript
  $(document).ready(function(){

  	//Check to see if the window is top if not then display button
  	$(window).scroll(function(){
  		if ($(this).scrollTop() > 100) {
  			$('.scrollToTop').fadeIn();
  		} else {
  			$('.scrollToTop').fadeOut();
  		}
  	});

  	//Click event to scroll to top
  	$('.scrollToTop').click(function(){
  		$('html, body').animate({scrollTop : 0},800);
  		return false;
  	});

  });
